name: AE phase scope probe
on: { workflow_dispatch: }
permissions: { contents: write }
jobs:
  p:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install psycopg2-binary
      - shell: bash
        env: { DATABASE_URL: "${{ secrets.NEON_DATABASE_URL }}" }
        run: |
          set -o pipefail
          mkdir -p db/audits
          python db/scripts/ae_phase_scope_probe.py 2>&1 | tee db/audits/ae_scope_probe.txt
      - if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "gha@users.noreply.github.com"
          git add db/audits/ae_scope_probe.txt
          git commit -m "phase scope probe" || echo none
          git pull --rebase --autostash -q || true
          git push
